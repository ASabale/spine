# HANDOFF

## Current State

Tree version **0.2.0**. Published as **spine-cli 0.2.0** on PyPI and GitHub (`v0.2.0`). Sit-down: `spine prime`. Exec: `spine run`.

Productionize route (full orchestration): tickets 02–09 resolved. Next: 10 atomic claims and concurrency model. Product ticket `01-dogfood-this-target` stays open HITL.

P3c delivered: inflight order from `contract.yaml`; `run` is `{cmd, hitl}` mappings. Claims table is ticket 10.

## Next Steps

Stranger install: `uvx --from spine-cli spine init`. Verify: `uv run pytest -q`.

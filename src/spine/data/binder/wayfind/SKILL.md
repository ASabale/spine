---
name: wayfind
description: Plan on the spine map. Use when the destination is foggy or work spans sessions.
---

# wayfind

Plan on `docs/spine/maps/` and the frontier under `docs/spine/tickets/`. Claim a ticket with the CLI before answering it. Done when the way is clear enough to mint a work item at `ready`.

```
spine status
spine new ticket --title "..." --type research
spine new --title "..." --ticket
spine claim tickets/NN-slug.md
```

Craft: `docs/spine/wires.yaml` concern `wayfind`.

# Changelog

## Unreleased

Productionize P0–P6 on the 0.2.0 tree: contract SSoT, real YAML eval/review
gates, SQLite claims, event log + revision binding, doctor recovery, structured
`run`, 50-worker harness, `make release`, `spine migrate`, dogfood crash
recovery. Product docs (`spec.md`, CONTEXT, README) describe that tree.
Claim is a one-winner lock. Publish/tag of a 0.3.0 is later.

## 0.2.0

First public spine-cli: init, prime, next, run, doctor, claim, set-status.

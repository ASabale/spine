# Changelog

## Unreleased

## 0.3.0

Productionize: contract SSoT, real YAML eval/review gates, SQLite one-winner
claims, event log + revision binding, doctor recovery, structured `run`,
50-worker harness, `make release`, `spine migrate`, dogfood crash recovery.
Product docs (`spec.md`, CONTEXT, README) match that tree.

## 0.2.0

First public spine-cli: init, prime, next, run, doctor, claim, set-status.

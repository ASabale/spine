# Changelog

## Unreleased

Productionize P0–P5: contract SSoT, real eval/review gates, SQLite claims,
event log + revision binding, doctor recovery, structured `run`, 50-worker
harness, `make release`, `spine migrate`.

## 0.2.0

First public spine-cli: init, prime, next, run, doctor, claim, set-status.
